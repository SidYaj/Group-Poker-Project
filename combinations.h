#ifndef COMBINATIONS_H
#define COMBINATIONS_H
#include <vector>
#include <algorithm>
#include "player.h"
#include "community.h"

std::vector<std::vector<int> > combinations(std::vector<int> cards); // to find all 5 card combinations to pass to function check_hand to see what hand a player has

#endif
